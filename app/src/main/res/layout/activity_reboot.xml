<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <EditText
        android:layout_marginTop="50dp"
        android:id="@+id/et_offset_time"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="亮屏时间"
        />
    <TextView
        android:layout_toRightOf="@id/et_offset_time"
        android:layout_alignBottom="@id/et_offset_time"
        android:paddingBottom="4dp"
        android:layout_marginLeft="8dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:text="ms"/>

    <Button
        android:id="@+id/btn_repeat_reboot"
        android:layout_below="@id/et_offset_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="无限重启"/>
    <Button
        android:id="@+id/btn_stop_reboot"
        android:layout_below="@id/btn_repeat_reboot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="停止重启"/>


</RelativeLayout>
